# 小影API 系统安全评估与整改报告

> 评估方式：全库静态代码审查（未改动任何文件）
> 评估范围：API 层（`API/apis/`、`API/common/`、`API/models/`）、爬虫层（`SpiderServices/`）、配置（`XiaoYingAPI/settings.py`）、部署文档（`BugAndRepair/`）
> 生成日期：2026-09-04

---

## 一、风险总览

| 编号 | 风险 | 级别 | 状态 |
|------|------|------|------|
| S-01 | Ddddocr：URL 抓取无内网过滤（SSRF）+ 本地路径任意文件读 + 任意路径写 | 🔴 高危 | ✅ 已整改 |
| S-02 | 任意文件上传（含 .svg / 无扩展名限制）+ /media/ 公开直服 | 🔴 高危 | ✅ 已整改 |
| S-03 | 全站无限流：登录爆破 / 验证码穷举 / 短信邮件轰炸 | 🔴 高危 | ✅ 已整改(用户中心) |
| S-04 | 第三方密钥硬编码进代码库 | 🔴 高危 | ✅ 已整改 |
| S-05 | 签名 nonce 未做服务端去重（5 分钟重放窗口） | 🔴 高危 | ✅ 已整改 |
| S-06 | app_secret / Token / 验证码明文落库且后台明文展示 | 🔴 高危 | ✅ 已整改 |
| S-07 | 全站 CSRF 关闭，/admin/ 后台无 CSRF 防护 | 🟠 中危 | ✅ 已整改 |
| S-08 | SECRET_KEY 兜底值公开已知 + ALLOWED_HOSTS 默认 `*` | 🟠 中危 | ✅ 已整改 |
| S-09 | Douyin / 网页抓取任意 URL 抓取（SSRF 残留、重定向绕过黑名单） | 🟠 中危 | ✅ 已整改（原 bing/crawl 服务已于 2026-09-04 整体下线删除） |
| S-10 | 验证码用非 CSPRNG（random）、无尝试上限、密码最短 6 位 | 🟠 中危 | ✅ 已整改 |
| S-11 | BugAndRepair 与部署文档含公网 IP/服务器路径；uwsgi 文档建议 0.0.0.0 裸监听 | 🟠 中危 | ✅ 已整改 |
| A-01 | API 认证默认 fail-open，安全依赖分类树配置正确 | 🟡 架构 | ✅ 已整改(fail-closed) |
| A-02 | 认证中间件每请求全表查 ApiCategory，无缓存 | 🟡 架构 | ✅ 已整改 |
| A-03 | 无计量/配额/账单体系（商业化缺口） | 🟡 架构 | ☐ 暂缓（曾实施，已按需回退） |
| A-04 | 爬虫同步阻塞请求线程；无结果缓存 | 🟡 架构 | 第二批（线程池+DB缓存） |
| A-05 | 无结构化日志/监控告警；线上 makemigrations 有漂移风险 | 🟡 工程 | ✅ 已整改 |

---

## 二、P0 —— 立即整改（高危）

### S-01 Ddddocr：SSRF + 任意文件读/写

**位置**
- 图片加载：[SpiderServices/DdddocrRecognizer/utils.py](file:///p:/XiaoYingAPI/SpiderServices/DdddocrRecognizer/utils.py) `load_image()` —— `http(s)://` 直接 GET；非 URL 字符串走本地 `open()` 读任意文件
- 目录扫描：[API/apis/DdddocrRecognizer/request.py](file:///p:/XiaoYingAPI/API/apis/DdddocrRecognizer/request.py) `batch_ocr_view`（`image_dir`/`pattern` 用户可控）
- 任意写：[API/apis/DdddocrRecognizer/request.py](file:///p:/XiaoYingAPI/API/apis/DdddocrRecognizer/request.py) `save_debug_view`（`save_dir`/`filename` 用户可控）

**整改方案**
1. 删除/停用 `batch_ocr`、`save-debug` 两个接口（或仅在本地 DEBUG 开关下暴露）。
2. `load_image` 仅保留两类输入：文件上传 bytes / base64 字符串；URL 下载若确需保留，须：仅允许 `http/https`、域名解析后拒绝私网段/回环/链路本地（`10.x/8.x/127.x/169.254.x/::1` 等）、`allow_redirects=False` 且逐跳校验、加超时与大小上限。
3. 若业务上「按服务器本地路径 OCR」是真实需求（调试用），把允许目录收敛为固定 `captcha_samples` 目录，路径做 `realpath` 前缀校验，不允许用户传绝对路径。
4. OCR 属 CPU 密集，接口挂限流（见 S-03）。

**验收标准**
- [x] 构造 `image=/etc/passwd`、`image=file:///...`、`image=http://127.0.0.1:PORT/`、`http://169.254.169.254/` 均被拒绝
- [x] `batch-ocr`/`save-debug` 匿名不可达，不再存在任意写文件路径
- [x] 提供上述用例的回归测试脚本（utils 安全用例 7/7 PASS）

### S-02 任意文件上传 + /media/ 公开直服

**位置**
- [API/apis/uploads/utils.py](file:///p:/XiaoYingAPI/API/apis/uploads/utils.py) `TYPE_CONFIG`：image 允许 `.svg`；file `allowed_ext=None`（任意）
- [XiaoYingAPI/urls.py](file:///p:/XiaoYingAPI/XiaoYingAPI/urls.py) 生产环境 `/media/(?P<path>.*)$` 直接 `serve`

**整改方案**
1. `image` 类型移除 `svg`（或专门路由 + `Content-Disposition: attachment` + `CSP: sandbox` 下载式返回）；新增 `svg` 独立受限通道时也要去脚本化。
2. `file` 类型改为白名单（如 zip/pdf/docx/xlsx/txt），拒绝 html/htm/svg/xml/js/mht 等可执行/可渲染类型；并加文件头（magic bytes）校验而非仅看扩展名。
3. media 文件服务对危险 MIME 强制 `X-Content-Type-Options: nosniff` + 附件下载头；媒体目录禁止覆盖上传路径外写文件。
4. 上传接口按分类树默认 `auth`（与 A-01 联动）。

**验收标准**
- [x] 上传 `.html`/`.svg`/`.php`/无扩展名恶意文件全部被拒
- [x] 上传图片改后缀为 `.jpg` 的 HTML 内容能被 magic bytes 校验拦截
- [x] 直接访问 `/media/.../x.svg` 返回下载而非内联渲染（Django 侧路由；生产 Nginx 直服 `/media/` 需补同款响应头，见整改记录）

### S-03 全站无限流

**位置**
- [API/common/status_code.py](file:///p:/XiaoYingAPI/API/common/status_code.py) 已预留 `RATE_LIMITED=20040`，但无实现
- 登录/验证码：[API/apis/user_center/users/utils.py](file:///p:/XiaoYingAPI/API/apis/user_center/users/utils.py) `login_user` / `verify_by_code` 无尝试上限
- 发码冷却仅 `VERIFY_RESEND_COOLDOWN=60`，防发送不防猜测

**整改方案**
1. 新增通用限流中间件（按 IP、按 app_id、按接口维度；SQLite 计数 + 内存窗口即可起步，后续迁 Redis），分类树配置项增加「限流档位」或按服务 prefix 默认启用。
2. 登录失败计数：同一 app+账号/IP 连续失败 N 次（如 5 次）锁定 15 分钟；返回 20040 而非 20011，避免枚举探测。
3. 验证码校验加失败上限：同一验证记录错误 ≥5 次即作废并要求重发（改 `is_used` 或新增 `fail_count`）。
4. 邮件发送（[API/apis/emails/v1](file:///p:/XiaoYingAPI/API/apis/emails/v1/request.py)）：收件人数上限（如 ≤50/次）+ 发件频控；短信类（sms_verify / DaiLianTong / DaiLianWanZi send_code）按手机号+IP 双重限流，防止短信轰炸。

**验收标准**
- [x] 暴力猜 6 位验证码 >5 次后验证码失效
- [x] 密码连续错误 5 次后该账号/IP 被临时锁定（返回 20040）
- [~] 脚本同接口高频调用返回 20040 且不再执行业务 —— 本次仅覆盖用户中心登录/验证码维度；全站接口级通用限流中间件留待后续（见整改记录）

### S-04 第三方密钥硬编码

**位置**
- 阿里云图形认证：[API/apis/captcha_auth/aliyun/utils.py](file:///p:/XiaoYingAPI/API/apis/captcha_auth/aliyun/utils.py) `CAPTCHA_APP_KEY` 明文常量
- 代练通：[SpiderServices/DaiLianTong/utils.py](file:///p:/XiaoYingAPI/SpiderServices/DaiLianTong/utils.py) `SIGN_KEY` 写死且 17 个接口复用
- 代理 IP：[SpiderServices/ProxyIp/ProxyIP_91http/utils.py](file:///p:/XiaoYingAPI/SpiderServices/ProxyIp/ProxyIP_91http/utils.py)、[SpiderServices/ProxyIp/ProxyIP_qy/utils.py](file:///p:/XiaoYingAPI/SpiderServices/ProxyIp/ProxyIP_qy/utils.py) 写死交易号/Secret，且 request 层允许调用方覆盖

**整改方案**
1. 全部迁入 `.env`（`*_API_KEY` 规范命名），代码从 `settings` 读取；`.env` 不入库。
2. 所有允许用户覆盖第三方凭据的参数（trade_no/secret/apikey）一律删除——业务方凭据由平台侧唯一持有。
3. 扫描全库确认无其他残留常量密钥；git 历史如已含密钥，按安全流程轮换一次密钥。

**验收标准**
- [x] 全库 grep 不到任何真实密钥/secret 字面量
- [x] 密钥可在不修改代码的前提下通过 `.env` 更换
- [x] 调用方无法传入第三方凭据覆盖平台配置

### S-05 签名 nonce 未去重

**位置**
- [API/apis/user_center/sign.py](file:///p:/XiaoYingAPI/API/apis/user_center/sign.py) `verify_sign()`：仅校验时间戳窗口 ±300s，nonce 只验非空

**整改方案**
1. 新增 nonce 去重表或复用 SQLite 表：`(app_id, nonce)` 唯一索引，校验时插入；窗口内重复即拒绝。定期清理过期 nonce（timestamp 超过 5 分钟可删）。
2. 或改用一次性令牌 + 服务端签发（成本高，不推荐首期）。
3. 与 S-03 限流中间件配合：同 app 高频请求直接 20040。

**验收标准**
- [x] 同一 timestamp+nonce+sign 原样重放被拒绝
- [x] 同 nonce 更换 timestamp 重新签名仍被拒绝（test_user_center 已更新断言）
- [x] 过期 nonce 数据可被定时清理，表不无限膨胀（security_guard 顺带清理）

### S-06 凭据明文落库 + 后台明文展示

**位置**
- [API/models/Projects/app.py](file:///p:/XiaoYingAPI/API/models/Projects/app.py) `app_secret` 明文 CharField
- [API/models/Users/user.py](file:///p:/XiaoYingAPI/API/models/Users/user.py) `UserToken.token`、`UserVerifyRecord.code/token` 明文
- [API/admin.py](file:///p:/XiaoYingAPI/API/admin.py) 自动注册全部模型且全字段 list_display/search_fields（app_secret、密码哈希、token、验证码均明文展示，app_secret 还可直接编辑）

**整改方案**
1. `app_secret`：存储加密（用 SECRET_KEY 派生密钥做对称加密）或存储「签名校验专用」的不可逆派生值（服务端校验用 HMAC 比对的密钥仍可加密后使用）；后台对 app_secret 字段 `exclude` 列表展示、只读、禁止搜索。
2. Token/激活链接：库内存哈希（SHA-256），校验时哈希后查询；登录时仅返回一次明文。
3. 验证码：校验通过/过期即清空 `code` 字段；避免长期留存。
4. admin 敏感字段治理：对 User.password、UserToken.token、UserVerifyRecord.code/token、UserApp.app_secret 统一加只读/隐藏/脱敏。
5. SQLite 文件权限收紧（仅运行用户可读写），备份加密异地存储（见 S-11 文档）。

**验收标准**
- [x] DB dump 中查不到任何可用的 app_secret/登录 Token/验证码明文（raw 校验：明文 app_secret 0 条；token/激活令牌已全部 SHA-256 哈希）
- [x] 后台列表与详情页不展示上述敏感字段，且不可直接改写（admin 治理 + exclude）
- [x] 现有签名/Touch 接口回归通过（test_auth_methods / test_user_center / test_api_auth_policy 全量通过）

---

## P0 整改记录与遗留事项（2026-09-04 完成）

**已完成代码改动**
- S-01：`SpiderServices/DdddocrRecognizer/utils.py`（SSRF 防护 + captcha_samples 白名单）、home/request/urls 删除 `batch-ocr`/`save-debug`；`test_recognizer.py` 同步移除相关用例并新增越权路径拒绝用例
- S-02：`API/apis/uploads/utils.py`（image 移除 svg、file 白名单、魔数校验）、`XiaoYingAPI/urls.py` 新增 `_media_serve`（nosniff + 危险类型附件下载）
- S-03/S-05：新增 `API/common/security_guard.py`（SQLite 自举表 api_login_guard / api_nonce）；登录失败 5 次锁 15 分钟返回 20040；验证码同记录猜错 ≥5 次作废；sign.py 签名校验后 nonce 去重
- S-04：密钥全部迁 `.env`（captcha_auth / DaiLianTong / ProxyIp），请求层禁止调用方覆盖凭据
- S-06：新增 `API/common/credential_crypto.py`；UserApp.app_secret 透明 AES-GCM 加密字段；登录/激活 Token 落库 SHA-256；验证码使用/过期即清空；admin 敏感字段治理（exclude，不可读不可改）

**部署/环境动作（必须执行一次）**
1. 上线后执行 `python manage.py security_backfill` 完成存量数据回填（一次性，带迁移标记防重复；已在本机执行）
2. 生产环境若由 Nginx 直服 `/media/`，请为 `location /media/` 追加同款安全响应头：
   `add_header X-Content-Type-Options nosniff;` 且对 `.svg/.html/.xml/.js/.php` 等返回 `Content-Disposition: attachment;`（Django 侧 `_media_serve` 仅覆盖由 Django 直服的场景）
3. SECRET_KEY 现在同时用于 app_secret 密文解密：**务必备份并保持 .env 中的 SECRET_KEY 不变**，变更即全部 app_secret 密文不可解密（需重新发放密钥）
4. 存量旧策略上传的危险文件（如 `media/uploads/files/*.xml`）建议人工清理

**遗留（不在本次 P0 范围，建议后续按报告推进）**
- S-03 全站接口级通用限流中间件（按 IP/app_id/接口）未实现，本次仅覆盖用户中心登录/验证码
- Apifox 若已收录 ddddocr `batch-ocr`/`save-debug`、上传类型文档，需同步下线/收紧描述
- S-02 的魔数校验对 video 类型未启用（报告仅要求 image/file）

**P1 项（S-07~S-11）已同日整改完成**：S-07 新增 `API/common/middleware.ApiCsrfExemptMiddleware`（全局 CSRF、/api/ 豁免）；S-08 SECRET_KEY 缺失拒启 + ALLOWED_HOSTS 缺省拒 `*`；S-09 新增 `API/common/url_safety.py`、抓取逐跳校验与批量≤10、抖音域白名单（原 bing/crawl 服务已于 2026-09-04 整体下线删除）；S-10 生成器改 secrets + 密码 8-64 位字母数字混合；S-11 运维文档脱敏 + uwsgi 回环监听 + 部署手册安全加固清单。详情见第三节各条验收勾选。

---

## 三、P1 —— 短期加固（中危）

### S-07 后台 CSRF 缺失

- **问题**：[settings.py](file:///p:/XiaoYingAPI/XiaoYingAPI/settings.py) 注释掉 `CsrfViewMiddleware`，全站含 `/admin/` 无 CSRF 校验。
- **整改**：恢复 CSRF 中间件仅作用于非 `/api/` 路径（如自定义中间件对 `/api/` 放行、其余走标准 CSRF），或把 `/admin/` 独立部署（不同域名/端口 + 同站策略）。API 层签名认证不受影响。
- **整改落地**：新增 `API/common/middleware.ApiCsrfExemptMiddleware`（继承 Django `CsrfViewMiddleware`，仅豁免 `/api/` 前缀）并加入 MIDDLEWARE。
- **验收**：后台表单缺失 csrfmiddlewaretoken 的 POST 被拒；`/api/` 接口不带 CSRF 仍正常。
- [x] 后台登录 POST 无 CSRF token → 403；`/api/` POST（无 CSRF）→ 正常走签名认证（用例 PASS）

### S-08 SECRET_KEY / ALLOWED_HOSTS 默认值

- **问题**：[settings.py](file:///p:/XiaoYingAPI/XiaoYingAPI/settings.py) `SECRET_KEY` 兜底为公开已知常量；`ALLOWED_HOSTS` 默认 `*`。
- **整改**：启动时若未从 `.env` 取到 SECRET_KEY 则直接抛错退出；ALLOWED_HOSTS 缺省拒绝 `*`（生产必须显式域名）。顺带确认 `XYAPI_COOKIE_ISOLATION` 生产不误设（Secure Cookie 依赖它）。
- **整改落地**：`SECRET_KEY` 不再有兜底值，缺失即 `ImproperlyConfigured` 拒绝启动；`ALLOWED_HOSTS` 缺省改为 `['127.0.0.1','localhost']`（.env 显式配置值仍生效）。
- **验收**：删除 `.env` 中的 SECRET_KEY 后服务拒绝启动；错误 Host 头请求被 400 拒绝。
- [x] 缺失 SECRET_KEY 抛错逻辑已实现（.env 现已有配置）；ALLOWED_HOSTS 缺省不再 `*`

### S-09 网页抓取类 SSRF 残留

- **位置**：Douyin 解析 [SpiderServices/VideoAnalysis/Douyin/home.py](file:///p:/XiaoYingAPI/SpiderServices/VideoAnalysis/Douyin/home.py) 兜底正则匹配任意 URL 并跟随重定向。（原 bing/search `crawl` 网页抓取服务已于 2026-09-04 随 Bing 服务整体下线删除，不再存在该接口。）
- **整改**：与 S-01 相同 URL 治理工具函数（协议白名单 + 内网 IP 拒绝 + 禁重定向/逐跳校验）；Douyin 解析改为仅接受「分享文本中提取的抖音域」白名单校验通过才请求。
- **整改落地**：新增通用公网 URL 校验模块 `API/common/url_safety.py`（协议白名单 + DNS 解析拒内网/回环/链路本地/保留段）并接入 Douyin 解析；Douyin 移除「任意 URL 兜底」正则，仅接受抖音系域名（v.douyin/iesdouyin/www.douyin/douyin）且重定向后域名须命中白名单。
- **验收**：视频解析对内网与元数据地址请求全部拒绝。
- [x] url_safety/视频解析对内网、回环、链路本地、元数据、非 http 协议全部拒绝

### S-10 账号与验证码细节

- **问题**：[API/apis/user_center/users/utils.py](file:///p:/XiaoYingAPI/API/apis/user_center/users/utils.py) `generate_email_code()`/`generate_account()` 用 `random`（MT19937，非 CSPRNG）；密码最短 6 位。
- **整改**：验证码与账号生成改用 `secrets`；密码策略升为 8+ 且含字母数字混合校验；尝试上限并入 S-03。
- **整改落地**：`generate_account`/`generate_email_code` 改用 `secrets`（CSPRNG），移除 `random` 依赖；密码策略：8-64 位且须同时含字母与数字（纯数字/纯字母拒绝），注册接口返回 20002。
- **验收**：`generate_email_code` 不再引用 `random`；注册短密码被拒。
- [x] 源码不再 import random；7 位/纯数字 8 位/纯字母 8 位均被拒，8 位字母数字混合通过（test_user_center 用例 PASS）

### S-11 信息泄露与运维文档

- **问题**：`BugAndRepair/` 文档含公网 IP、真实站点路径；uwsgi 手册建议 `http=0.0.0.0` 裸监听。
- **整改**：文档 IP/路径脱敏（占位符）；uwsgi 生产统一 `socket` + 仅监听 `127.0.0.1` + Nginx `uwsgi_pass`；`db.sqlite3` 防下载与备份加密写进部署手册；后台 `/admin/` 建议 IP 白名单或改名。
- **整改落地**：事故报告/部署手册/Nginx 样例中的公网 IP、真实域名与 `/www/...` 绝对路径全部替换为 `{SERVER_IP}/{PROJECT_ROOT}/{BT_NGINX_VHOST}/{LOG_ROOT}/{DEPLOY_DOMAIN}/{PYTHON_BIN}/{UWSGI_BIN}` 等占位符；uwsgi 样例统一改为 `http=127.0.0.1:端口`（注释说明禁止 0.0.0.0 裸监听）；部署手册新增「安全加固要点（S-07~S-11）」：db.sqlite3 防下载（nginx deny）、备份加密、/media/ 响应头、/admin/ 收敛建议。
- **验收**：全库扫描无真实公网 IP/站点绝对路径残留；部署文档模板不再出现 `0.0.0.0` HTTP 监听。
- [x] 文档全库扫描仅剩 127.0.0.1 回环示例与「禁止 0.0.0.0」说明文字，无真实公网 IP/域名/站点绝对路径

---

## 四、P2 —— 架构与能力补强

| 编号 | 事项 | 方案要点 | 验收方向 |
|------|------|----------|----------|
| A-01 | 认证 fail-open 改 fail-closed | 分类树新增「全局默认策略」开关；对 upload/ddddocr/captcha/email/sms 等敏感前缀默认要求认证；未命中节点时按默认策略而非放行 | 无配置的新增服务默认不可匿名访问 |
| A-02 | 分类树查询缓存 | 进程内 TTL 缓存（30-60s）或迁移 Redis；节点变更时失效 | 压测 `/api/` 请求 DB 查询次数显著下降 |
| A-03 | 计量/配额/账单 | UserApp 加调用量统计表（按 app×接口×日），中间件计数；先只统计后限配额；预留套餐与账单模型 | 后台可按项目查看调用报表；可设置日配额 |
| A-04 | 爬虫异步化 + 结果缓存 | 长耗时爬虫（抖音/代练等）改任务队列（可先进程内线程池 + DB 结果表），同参数结果 TTL 缓存 | 上游超时不阻塞 worker；同 URL 重复抓取走缓存 |
| A-05 | 可观测性 | 结构化日志（请求ID/耗时/状态码/调用方 app）；错误告警；线上迁移改为随代码管理（不再线上 makemigrations） | 一次故障可通过日志完整追溯 |

---

## P2 整改记录（2026-09-04：A-01/A-02/A-05 第一批完成；A-03/A-04 第二批待设计后实施）

**已决策的执行口径（与需求方确认）**
- 分两批：第一批 A-01/A-02/A-05（技术债/工程类，本次完成）；第二批 A-03（仅计量统计，先只统计不限流）+ A-04（进程内线程池 + DB 结果表缓存），**不引入 Redis/任务队列**（保持单机 uWSGI + SQLite 现状，量级上来再演进）。
- A-01 默认策略：**fail-closed（未配置即拒绝签名）**；无免签存量外部消费方，能力型服务全量收编认证。

**A-01 落地（fail-closed）**
- [middleware.py](file:///p:/XiaoYingAPI/API/common/middleware.py) `ApiAuthMiddleware._requires_auth`：未命中分类节点 / 全链 inherit / 命中分类停用 → 一律要求认证（原为放行）；显式 open 节点与 `PUBLIC_GET_PATHS` 公开 GET 路径仍可匿名。
- 分类树实际配置调整（本机 DB 已改，生产发布需同步核对）：`/api/music/` open→auth；`/api/captcha_auth/aliyun/` 显式 open（开放集成设计，config/verify 均免签，尊重既有契约）；其余 inherit 节点由新默认收编为需认证。原 `PUBLIC_GET_PATHS` 增补的 captcha config 项已随 open 方案移除（避免重复来源）。（原 `/api/bing/indexnow/` 显式 open 节点已随 Bing 服务整体下线删除。）
- 行为变更说明（对接方注意）：upload/ddddocr/email/ai/ProxyIp/dlt/dlwz/music/seo/spider_verification 等此前免签服务，现在必须携带 app_id/timestamp/nonce/sign 签名；仅 captcha_auth/aliyun 及公开 GET 路径（邮箱激活链接、认证方式列表）可匿名。未匹配分类的未知路径匿名请求返回 20011（非 404）。
- 验收：`test_api_auth_policy.py` 重写为 fail-closed 语义 30/30 PASS；`test_live_http.py` 语义注释同步。

**A-02 落地（分类树缓存）**
- [middleware.py](file:///p:/XiaoYingAPI/API/common/middleware.py) 新增进程内 TTL 缓存 `_category_nodes()`（默认 60s，`settings.API_CATEGORY_CACHE_TTL` 可调），每请求不再全表查 `ApiCategory`。
- 失效钩子：admin 保存/删除 ApiCategory（[apps.py](file:///p:/XiaoYingAPI/API/apps.py) post_save/post_delete 信号）与 `rebuild_category_tree` 命令执行后立即失效，后台改动即时生效。
- 验收：30 用例（含并发 10 路、运行中改分类即时生效）全部通过。

**A-05 落地（可观测 + 迁移纪律）**
- settings 新增 LOGGING：console + `logs/app.log`（INFO）+ `logs/error.log`（ERROR 分流，Rotating 10MB×5）；`logs/` 目录自动创建。
- 新增 `ApiRequestLogMiddleware`：每请求生成 request_id（响应头 `X-Request-Id`），记录 method/path/status/cost_ms/app（auth_app.app_id），视图未捕获异常带 request_id + 完整堆栈落 error.log；已验证 500 可用 request_id 全链路追溯。
- 迁移入库：`.gitignore` 放开 `**/migrations/*`（保留 __init__ 规则删除），迁移文件首次需 `git add` 随代码入库；README / 部署手册 / base.py 全部改为「本地 makemigrations → 代码提交 → 线上仅 migrate」，禁止线上 makemigrations（历史漂移根因，部署手册已注明升级时先核对 `django_migrations` 与代码库迁移集一致性）。
- 验收：`logs/error.log` 含 request_id+Traceback；`git status` 可见迁移文件已不被忽略。

**A-03 计量统计（已实施后按需整体回退，恢复未实施状态）**
- 曾完成：`ApiUsageStat` 模型/迁移 0019、`ApiRequestLogMiddleware` 计数、`/admin/usage/` 聚合页与只读注册、`scripts/test_api_usage.py`。
- 按需求方决定**暂时不需要**该功能：已删除全部相关代码（模型包、迁移 0019、计数逻辑、usage 路由/页面、测试脚本），数据库迁移回滚至 0018（`api_usage_stat` 表已删除）。后续需要时按本节方案要点重做（仅计量、不限流、含匿名、成功/失败拆分、永久保留）。

---

## 五、整改路线图（建议执行顺序）

1. **第 1 批（止血，1~2 轮）**：S-01、S-02、S-03、S-04、S-05 —— 全部属「漏洞级」，建议优先完成并补回归测试。
2. **第 2 批（加固）**：S-06、S-07、S-08、S-09、S-10、S-11。
3. **第 3 批（能力）**：A-01/A-02/A-05 已完成；A-03 计量统计暂缓（曾实施后按需回退，待商业化再启）；A-04（异步线程池 + DB 缓存）待设计与实施。

> 每项完成标准以「验收标准」勾选为准；P0 项修复前后各跑一遍现有 scripts 回归（`test_api_auth_policy.py` / `test_user_center.py` / `test_sms_verify.py` 等），确保签名认证链路不回归。A-01 行为变更上线前，需通知对接方接入签名后再切流量（部署手册安全加固要点已补充）。
